/** Coordinate on a desktop screenshot. */
export type Point = {
    /** X coordinate on the desktop screenshot. */
    x: number;
    /** Y coordinate on the desktop screenshot. */
    y: number;
};
