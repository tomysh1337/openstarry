# OpenStarry NextGen 项目约定

本文件记录用户在本项目对话中确认的产品需求。后续开发以这些确认和用户最新修正为依据；未明确的实现细节不应冒充已确认需求。

## 产品和交付

- 项目由 APIX 更名为 OpenStarry NextGen，作者和品牌使用 tomysh。
- Windows 10、Windows 11 上作为一体化软件运行，不要求用户单独操作后端或终端。启动、退出、诊断、更新均在软件内完成。
- 使用用户提供的图片作为图标。安装包初始为空数据，不打包开发者的聊天、配置、令牌等测试数据。
- 关闭窗口默认缩小到托盘，在设置中选择退出行为；卸载时由用户选择保留或删除本地数据。
- 用户已授权本项目直接实现、测试、commit、push 和发布 Release；不要把构建成功当作完整功能验证。
- 对需求不明确的部分集中询问；按最终确认继续工作。本项目不主动使用子 Agent。

## 本地数据和凭据

- 聊天历史、附件、设置和任务历史优先保存在本机，尽量降低文件体积；使用压缩备份、附件去重和离线保留机制。
- 回收站保存超过 45 天后仅提醒，不自动永久删除；使用阿里云等公共 NTP 校准时间。
- 保留 Windows 凭据保护和 Windows Hello，用户可自行选择密码保护方式。
- 云同步使用用户配置的服务器、认证和 HTTPS。不要把服务器密码、API 密钥、访问令牌写入仓库、日志或示例。

## Agent

- 支持自动续写和长任务，并提供运行时长、回合和重复失败上限。
- Windows 端集成用户提供的 computer-use 包。用户提到电脑控制或完成任务确有需要时，按设置模式运行；提供自动允许、每次询问和关闭。
- 保留用户确认的不可逆操作提示选项。电脑控制相关设置集中放在下方 Agent 板块。

## 手机端（2026-09-16 修正）

- 手机端应是能独立创建会话、选择模型、发送消息和接收回答的聊天客户端；此前仅浏览同步历史的实现未满足需求。
- 手机可省去电脑控制、本机文件系统操作等桌面专用能力。
- 手机与电脑双向互通聊天历史；断网保留本机记录，恢复网络后同步。
- 用户已确认，同步供应商、模型、温度、主题与角色提示词等通用聊天偏好；API 密钥各设备保存，桌面专属权限保留在电脑。
- 2026-09-19：手机 GUI 与电脑端的配色、卡片、聊天输入栏和设置层次保持一致，同时适配手机触控、窄屏和软键盘。
- 2026-09-19：设置补齐实际可用的 Agent 调用能力；手机和电脑都提供完整 IDE 工作区，参考 IDEA 的项目树、多标签编辑、搜索、运行输出和关联项目的 Agent 面板。
- IDE 提供红色删除、绿色新增的代码审查；Agent 修改先生成待审查内容，用户可接受或撤销。
- 手机 JavaScript / 网页在本机预览，Python、Java 等项目连接用户配置的执行服务器；项目执行令牌、工具权限与 API 密钥只保存在当前设备。
- 2026-09-19 追加：IDE 工作台参考 VS Code 的活动栏、文件树、标签页、输出和状态栏；Agent 面板参考 IDEA 的 Claude Code 插件，集中呈现会话、工具记录、文件上下文和底部输入区。
- 后续视觉修正：用户更偏好 IDEA 新 UI，工作台整体采用 IDEA 式工具窗口、面板层次、字号和间距；Agent 延续 IDEA 中 Claude Code 插件的组织方式。
- 2026-09-19 追加：用户要求网页、手机、电脑都有账户注册与登录入口；手机与电脑可双向关联。关联范围（同账户同步、扫码配对或远程操作）仍待用户补充，不将其默认为已确认的远程控制需求。
- 用户希望先查看当前 IDE 再补充布局需求；已指出 Agent 原生下拉框外观割裂，并要求提供相关源码与类似 Codex 预览的实现说明。
- Agent 底部未配置模型时，点击「请先配置模型」直接进入供应商配置；保存配置后恢复模型选择。手机设置开关需保持完整轨道，圆点在开启、关闭状态都不越界。

## 本轮修复与体验需求

- 修复设置卡片固定高度引起的文字、按钮溢出及大块空白，在窄窗口下自动换行和撑高。
- 整理同步配置入口及 Agent 设置层次，显示清晰的保存、连接与同步反馈。
- 添加轻量平滑的页面、卡片与弹窗过渡，尊重减少动态效果偏好。
- 修复拖动、缩放或滚动 GUI 时暂时失灵的问题，交互中不隐藏整个内容区。
- 供应商新增后仍可编辑名称、接口、模型列表与模型内容；修改密钥应是独立操作，不应只有 API 密钥弹窗。
- 验证供应商新增/编辑、手机聊天、双向同步、窗口调整及主要页面，明确记录验证范围和遗留问题。

- Agent 需要澄清项目需求时，聊天输入栏上方自动显示问题卡（选项和自由文本），并显示 Windows 系统通知；回答后继续原任务。

- 问题卡使用与聊天输入框对齐的宽版布局；补充说明选填，附加问题可跳过，不强制用户填写补充。

- 动效需要自然、连贯和有操作反馈：导航选中态跟随、弹窗柔和展开、问题卡展开与逐题过渡；历史刷新不重复播放所有消息动画，拖动滚动保持即时响应。

- 2026-09-19：Agent 工具窗口参考 `jetbrains-cc-gui` 的 IDEA 组织方式：会话标题栏、项目会话标签、平面消息时间线、工具执行行、底部输入区和任务状态条。只借鉴布局与交互节奏，OpenStarry 不打包参考项目的品牌名称、Logo 或图标。
- Agent 状态条显示当前任务、子 Agent 数量、工具调用数量及待审查文件的增删统计；修改统计直接使用现有红绿差异数据，点击待审查修改可进入审查面板。
- 2026-09-19 后续需求记录：供应商配置需要收进 Agent 侧边栏的小窗口。窗口应保留供应商名称、接口地址、API 密钥和模型 ID 编辑能力，保存或取消后回到原 Agent 会话；保存成功后刷新 Agent 模型选择，不再把用户带到整页供应商设置。
